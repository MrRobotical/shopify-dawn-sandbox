{% assign textareaContent = section.settings.custom-text | split: '\n' %}
{% assign cleanedContent = '' | split: '' %}

{% for line in textareaContent %}
  {% assign cleanedLine = line | replace: '\n', '' | strip %}
  {% if cleanedLine != '' %}
    {% assign cleanedContent = cleanedContent | push: cleanedLine %}
  {% endif %}
{% endfor %}

<pre>{{ customTags | json }}</pre>

{% schema %}
{
  "name": "Product Tag Msg",
  "settings": [
    {
      "type": "textarea",
      "id": "tag-name",
      "label": "Enter Tag Name"
    },
        {
      "type": "text",
      "id": "tag-text",
      "label": "Custom Msg when tag is present"
    }
  ], 

 "presets" : [
      {
        "category": "custom", 
        "name": "Product Tag Message"
      }
    ]

  
}

{% endschema %}


{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}